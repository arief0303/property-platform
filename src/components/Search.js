import React from "react";

const Search = () => {
  return (
    <input
      type="text"
      placeholder="Search property..."
      className="flex input input-bordered input-secondary w-full"
    />
  );
};

export default Search;
